---
status: superseded
phase: 08.1-transport-controls-and-lcd-display
source:
  - 08.1-01-SUMMARY.md
  - 08.1-02-SUMMARY.md
  - 08.1-03-SUMMARY.md
started: 2026-04-24T16:00:00Z
updated: 2026-04-24T16:05:00Z
superseded_reason: UI redesign via Claude Design handoff — entire Phase 8.1 visual layer will be replaced. UAT halted pending handoff bundle arrival.
---

## Current Test

[superseded — UAT halted pending Claude Design handoff]

## Tests

### 1. LCD Display Visible
expected: Launch the app and load a track. The LCD panel (275×43pt, black background, Winamp LCD-green text) should appear between the title bar and the seek bar. It shows: song title (top-left scrolling area), elapsed time digits (e.g. "0:03"), bitrate ("128"), kHz ("44"), and stereo/mono label ("STEREO").
result: pass

### 2. LCD Time Toggle
expected: Click the time display area (roughly the digit region on the left side of the LCD). The time readout should toggle between elapsed time ("0:12") and remaining time ("-3:44"). Click again to toggle back.
result: skipped
reason: UAT superseded by Claude Design handoff

### 3. LCD Title Scroll
expected: Load a track with a long title (more than ~30 chars). The title in the LCD should continuously scroll left, loop back with " *** " spacer, and repeat. Short titles should stay static.
result: skipped
reason: UAT superseded by Claude Design handoff

### 4. Seek Bar Tracks Position
expected: During playback, the green fill in the seek bar grows from left to right as the track plays. The bright thumb indicator stays at the right edge of the fill. The fill accurately represents the playback position (e.g. halfway through = 50% fill).
result: skipped
reason: UAT superseded by Claude Design handoff

### 5. Drag to Seek
expected: Click and drag on the seek bar while a track is playing. The fill should update immediately to where you drag (no spring-back). When you release, playback should jump to that position. The time display in the LCD should update to match.
result: skipped
reason: UAT superseded by Claude Design handoff

### 6. Volume Slider
expected: Drag the volume slider (left slider near transport buttons). Audio volume should change as you drag — louder to the right, quieter to the left. The slider fill should visually follow your drag immediately.
result: skipped
reason: UAT superseded by Claude Design handoff

### 7. Pan Slider
expected: Drag the pan slider (right slider near transport buttons). Audio should pan left or right as you drag. Center position produces balanced stereo. The slider fill should visually follow your drag immediately.
result: skipped
reason: UAT superseded by Claude Design handoff

### 8. Transport Buttons Layout
expected: Seven transport buttons appear in a row below the seek bar — Prev (⏮), Rew, Play/Pause, Stop, Fwd, Next (⏭), Eject (⏏). Each is ~23×18pt with visible NeoAero glass chrome (gradient fill, specular highlight, green-tinted rim glow).
result: skipped
reason: UAT superseded by Claude Design handoff

### 9. Transport Button Hover
expected: Hover the mouse over any transport button (without clicking). The rim glow around the button should dim noticeably (alpha reduces). Moving the mouse away should restore the full rim glow.
result: skipped
reason: UAT superseded by Claude Design handoff

### 10. Play/Pause Toggle
expected: Click the Play button (▶) — playback starts and the button label changes to ‖ (pause). Click it again — playback pauses and the button label changes back to ▶. Label should switch within 100ms of each click.
result: skipped
reason: UAT superseded by Claude Design handoff

### 11. Next Track
expected: With multiple tracks in the playlist, click the Next button (⏭ or →). Playback should jump to the next track. The LCD title should update to the new track name within one poll cycle (~100ms).
result: skipped
reason: UAT superseded by Claude Design handoff

### 12. Prev Track (Smart Prev)
expected: While a track is playing, click Prev (⏮). If you are more than a few seconds in, it should restart the current track. If you are near the beginning, it should jump to the previous track.
result: skipped
reason: UAT superseded by Claude Design handoff

### 13. Stop Button
expected: Click the Stop button (■) while a track is playing. Playback should stop. The seek bar should return to position 0. The play/pause button should show ▶.
result: skipped
reason: UAT superseded by Claude Design handoff

### 14. Eject Auto-Start
expected: Launch the app with an empty playlist. Click the Eject button (⏏). Select one or more audio files in the file picker. After the picker closes, playback should start automatically on the first file — the LCD updates and the seek bar begins moving. (Tests the WR-03 fix.)
result: skipped
reason: UAT superseded by Claude Design handoff

### 15. Spectrum Relocated to Viz Band
expected: The spectrum analyzer display should appear inside the main body of the Winamp window — in the visualization band (roughly top of the body area, 107×32pt wide). It should NOT appear at the very bottom of the window or outside the main player window.
result: skipped
reason: UAT superseded by Claude Design handoff

## Summary

total: 15
passed: 1
issues: 0
pending: 0
skipped: 14

## Gaps

[none yet]
