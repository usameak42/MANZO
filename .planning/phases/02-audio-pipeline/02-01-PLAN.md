---
phase: 02-audio-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - manzo-core/Cargo.toml
  - manzo-core/tests/fixtures/test.mp3
autonomous: true
requirements:
  - AUDIO-04

must_haves:
  truths:
    - "Cargo.toml contains mpg123-sys and cpal as runtime dependencies"
    - "tests/fixtures/test.mp3 exists as a valid MPEG Layer 3 file"
    - "cargo build exits 0 after dep additions"
  artifacts:
    - path: "manzo-core/Cargo.toml"
      provides: "runtime dependency declarations for mpg123-sys and cpal"
      contains: "mpg123-sys"
    - path: "manzo-core/tests/fixtures/test.mp3"
      provides: "CC0 5-second test MP3 fixture for integration tests"
  key_links:
    - from: "manzo-core/Cargo.toml"
      to: "manzo-core/src/lib.rs"
      via: "extern crate resolution at compile time"
      pattern: "mpg123-sys"
---

<objective>
Add mpg123-sys and cpal runtime dependencies to Cargo.toml, create the tests/fixtures directory, and generate a CC0 MP3 test fixture so the implementation plan (02-02) has a compilable dep graph and working test asset.

Purpose: Unblocks both the Rust implementation (needs crate resolution) and the integration test (needs a fixture file). Both are independent of each other and must land first.

Output:
- `manzo-core/Cargo.toml` — runtime deps added (mpg123-sys, cpal)
- `manzo-core/tests/fixtures/test.mp3` — CC0 5-second sine tone at 44100 Hz stereo
</objective>

<execution_context>
@/Users/usameak42/Coding/MANZO/.claude/get-shit-done/workflows/execute-plan.md
@/Users/usameak42/Coding/MANZO/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/usameak42/Coding/MANZO/.planning/ROADMAP.md
@/Users/usameak42/Coding/MANZO/.planning/STATE.md

<interfaces>
<!-- Current Cargo.toml — executor must see before editing -->
<!-- From manzo-core/Cargo.toml (full file): -->
```toml
[package]
name = "manzo-core"
version = "0.1.0"
edition = "2021"

[lib]
name = "manzo_core"
crate-type = ["staticlib"]

[build-dependencies]
cbindgen = "0.27"

[dev-dependencies]
# nothing yet — unit tests live in src/lib.rs

[profile.release]
opt-level = 3
lto = true
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add mpg123-sys and cpal to Cargo.toml</name>
  <files>manzo-core/Cargo.toml</files>
  <read_first>
    - manzo-core/Cargo.toml — read current state before editing; must preserve all existing sections
  </read_first>
  <action>
Insert a `[dependencies]` section between the `[lib]` block and `[build-dependencies]`. The exact additions:

```toml
[dependencies]
mpg123-sys = "0.1"
cpal = "0.15"
```

Constraints to preserve:
- `crate-type = ["staticlib"]` — must remain unchanged
- `lto = true` — must remain in `[profile.release]`
- `cbindgen = "0.27"` in `[build-dependencies]` — unchanged
- Do NOT add a `features` key to cpal — use the default feature set; CoreAudio backend is
  enabled by default on macOS

After editing, run `cargo fetch` (or `cargo build`) from `manzo-core/` to confirm crate resolution.
On macOS the mpg123-sys crate links against system libmpg123 via pkg-config or a bundled build;
if pkg-config fails, the crate ships its own bundled source that compiles automatically — no
Homebrew prerequisite is required.
  </action>
  <verify>
    <automated>cd /Users/usameak42/Coding/MANZO/manzo-core && grep -E "^mpg123-sys|^cpal" Cargo.toml && cargo fetch 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Cargo.toml contains line: `mpg123-sys = "0.1"`
    - Cargo.toml contains line: `cpal = "0.15"`
    - `[dependencies]` section exists in Cargo.toml (grep "^\[dependencies\]" returns a match)
    - `crate-type = ["staticlib"]` still present
    - `cargo fetch` exits 0 (crates resolve without network error)
  </acceptance_criteria>
  <done>Cargo.toml has mpg123-sys and cpal under [dependencies]; cargo fetch exits 0</done>
</task>

<task type="auto">
  <name>Task 2: Generate CC0 test MP3 fixture</name>
  <files>manzo-core/tests/fixtures/test.mp3</files>
  <read_first>
    - No prior file to read; this task creates a new binary asset
  </read_first>
  <action>
Create the directory and generate a 5-second CC0 sine tone MP3 using ffmpeg (available on macOS
via Homebrew). Run from the repo root:

```bash
mkdir -p manzo-core/tests/fixtures
ffmpeg -y -f lavfi -i "sine=frequency=440:duration=5" \
  -ar 44100 -ac 2 -c:a libmp3lame -q:a 5 \
  manzo-core/tests/fixtures/test.mp3
```

If ffmpeg is not available, try sox:

```bash
sox -n -r 44100 -c 2 manzo-core/tests/fixtures/test.mp3 synth 5 sine 440
```

The file must be:
- Format: MPEG Layer 3 (verified by `file` command output containing "MPEG")
- Sample rate: 44100 Hz
- Channels: stereo (2)
- Duration: ~5 seconds (at least 4.5 s)
- License: CC0 — synthesized tone, no copyright

Do NOT download from the internet — generate locally to guarantee CC0 provenance.
  </action>
  <verify>
    <automated>file /Users/usameak42/Coding/MANZO/manzo-core/tests/fixtures/test.mp3 | grep -i "MPEG\|mp3\|audio"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `manzo-core/tests/fixtures/test.mp3`
    - `file manzo-core/tests/fixtures/test.mp3` output contains "MPEG" or "Audio" (confirms valid MP3 header)
    - File size is between 20 KB and 200 KB (5-second MP3 at reasonable bitrate)
    - `ls -la manzo-core/tests/fixtures/test.mp3` shows non-zero size
  </acceptance_criteria>
  <done>manzo-core/tests/fixtures/test.mp3 exists as a valid ~5-second MPEG Layer 3 file, synthesized from a sine tone (CC0)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external registry → Cargo.lock | Crate downloads from crates.io; resolved hashes stored in Cargo.lock |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | crates.io dep resolution | accept | Cargo.lock pins exact versions and checksums; low risk for dev dependency fetch |
| T-02-02 | Repudiation | test.mp3 fixture | accept | File is synthesized locally (ffmpeg/sox); no third-party content; CC0 provenance from generation command |
</threat_model>

<verification>
1. `grep "mpg123-sys" manzo-core/Cargo.toml` — exits 0
2. `grep "cpal" manzo-core/Cargo.toml` — exits 0
3. `file manzo-core/tests/fixtures/test.mp3` — output contains "MPEG" or "Audio"
4. `cd manzo-core && cargo fetch` — exits 0
</verification>

<success_criteria>
- Cargo.toml has [dependencies] section with mpg123-sys = "0.1" and cpal = "0.15"
- tests/fixtures/test.mp3 is a valid ~5-second MPEG Layer 3 file
- cargo fetch resolves both new crates without error
</success_criteria>

<output>
After completion, create `/Users/usameak42/Coding/MANZO/.planning/phases/02-audio-pipeline/02-01-SUMMARY.md`
</output>
