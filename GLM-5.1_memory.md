# GLM-5.1 Session Memory — MANZO Project

Last updated: Sat Apr 25 2026

## Project Identity
- **Name:** MANZO — macOS Winamp-inspired music player
- **Stack:** Rust (audio core via manzo-core) + Swift/AppKit (UI via ManzoApp)
- **Location:** /Users/usameak42/Coding/MANZO
- **Target:** macOS 26+, Apple Silicon M4 Pro
- **Build:** SUCCEEDED — clean build as of last session

## What Is Complete (DO NOT TOUCH)
- `manzo-core/` — Rust audio engine, all phases 1–8 done
- FFI bridge — cbindgen generated header (`manzo-core/manzo_core.h`), working
- `ManzoMainWindow.swift` — generated by Claude Design. Sacred file. DO NOT TOUCH
- `ManzoPlaylistPanel.swift` — playlist UI, working
- `PlaylistRowView.swift`, `PlaylistTrack.swift` — playlist row components
- `AppDelegate.swift` — all P1 wiring complete:
  - Pause button → handlePlayPauseButton()
  - Status bar → live state (Playing/Paused/Stopped)
  - SeekBar → position sync + mouse drag → manzo_seek FFI
  - PL toggle → shows/hides playlistPanel
  - EQ toggle → TODO placeholder (no EQ panel built yet)
  - SpectrumView → real manzo_get_spectrum_bars FFT data

## What Is Next
**ManzoEQPanel.swift** — will be handed by the design tool.
Job: place it, wire it to AppDelegate and `manzo_set_eq` FFI. Nothing else.

## Workflow Rules (CRITICAL)
1. NEVER design or invent any UI component
2. NEVER write visual Swift code from scratch
3. If a UI file is handed → place it, wire it, nothing else
4. If something is missing → say "missing, needs design generation" and stop
5. `ManzoMainWindow.swift` → DO NOT TOUCH under any circumstance
6. `manzo-core/` → DO NOT TOUCH under any circumstance

## Key File Locations
| File | Path | Notes |
|------|------|-------|
| AppDelegate | `ManzoApp/ManzoApp/AppDelegate.swift` | 707 lines, all wiring |
| Main Window | `ManzoApp/ManzoApp/ManzoMainWindow.swift` | 830 lines, sacred |
| Playlist Panel | `ManzoApp/ManzoApp/ManzoPlaylistPanel.swift` | 417 lines |
| Playlist Row | `ManzoApp/ManzoApp/PlaylistRowView.swift` | 241 lines |
| Playlist Track | `ManzoApp/ManzoApp/PlaylistTrack.swift` | 42 lines |
| FFI Header | `manzo-core/manzo_core.h` | 96 lines, cbindgen output |
| Design Kit | `manzo-design-system/project/manzo_ui_kit.html` | 716 lines, has full EQ design |
| Design Tokens | `manzo-design-system/project/colors_and_type.css` | 251 lines |
| Project Config | `CLAUDE.md` | Constraints & workflow |

## EQ TODO — Exact Location
`AppDelegate.swift:140`:
```swift
// TODO: wire body.eqBtn.onToggle when ManzoEQPanel is implemented
```

## eqBtn Declaration
`ManzoMainWindow.swift:242` — property of `ManzoBodyView`:
```swift
let eqBtn = PillToggle(title: "EQ", isOn: true)
```
Accessed in AppDelegate via `body.eqBtn` (line 140).

## manzo_set_eq FFI Signature
From `manzo_core.h:54`:
```c
void manzo_set_eq(struct manzo_ManzoHandle *handle, const float *gains, float preamp);
```
- `gains` → pointer to 10 float values in dB (±12.0)
- `preamp` → additional preamp gain in dB

## Existing Wiring Patterns (follow these for EQ)
The PL toggle pattern in AppDelegate (lines 134–139) is the template to follow:
```swift
body.plBtn.onToggle = { [weak self] isOn in
    guard let self else { return }
    if isOn { self.playlistPanel?.makeKeyAndOrderFront(nil) }
    else     { self.playlistPanel?.orderOut(nil) }
}
```
EQ panel should follow the same pattern: `body.eqBtn.onToggle` → show/hide eqPanel.

## Design System — EQ Panel Reference
The full EQ panel design exists in `manzo_ui_kit.html` (lines 553–616):
- 10-band sliders (60Hz–16kHz) + preamp
- LED On/Auto toggle buttons
- Curve graph (Catmull-Rom SVG)
- Presets dropdown button
- dB scale labels (+12/0/−12)
- Navy textured backdrop
- Winamp-accurate ribbed square thumbs

## Critical Constraints (from spikes)
- One `.behindWindow` NSVisualEffectView at window root ONLY
- All inner panels: CALayer-only, `isOpaque = false`, no nested NSVisualEffectView
- P3 colors everywhere: `CGColor(colorSpace: .displayP3)` — no sRGB
- MTKView: `.rgba16Float` + explicit `CAMetalLayer.colorspace` (P3 not auto-configured)
- `shouldRasterize = true` requires `rasterizationScale = 2.0` on Retina
- MP3 gapless: trim exactly 529 samples from mpg123 decoder output
- Float32 pipeline end-to-end — no int16 conversion in audio path
- FFI surface: exactly 11 `manzo_*` functions via cbindgen C header
